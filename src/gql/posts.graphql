query getAllPosts {
	posts {
		id
		createdAt
		slug
		title
		coverImage {
			url
		}
	}
}

query getPostBySlug($slug: String!) {
	post(where: { slug: $slug }) {
		id
		title
		tags
		slug
		author {
			name
		}
		content {
			markdown
		}
		coverImage {
			url
		}
	}
}
