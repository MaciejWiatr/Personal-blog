query getAllPosts {
	posts {
		createdAt
		slug
		title
		coverImage {
			url
		}
	}
}

query getPostBySlug($slug: String!) {
	post(where: { slug: $slug }) {
		title
		tags
		slug
		author {
			name
		}
		content {
			markdown
		}
	}
}
